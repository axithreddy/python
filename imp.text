max(arr)
min(arr)
sum(arr)

#list comprehension
return [x for x in arr if x == max_value] #if you want to gett all max values of an array(duplicates)


if all (j % i == 0 for j in b):


class Solution:
    def isSubsequence(self, s: str, t: str) -> bool:
Means:
	•	class Solution: ➝ You’re defining a class called Solution.
	•	def isSubsequence(...) ➝ This is a method (a function inside a class).
	•	self ➝ refers to the instance (object) of the class.
	•	s: str, t: str ➝ means both s and t are expected to be strings.
	•	-> bool ➝ This method will return a Boolean (True or False).


# two pointers approach: Two pointers is an extremely common technique used to solve array and string problems. It involves having two integer variables that both move along an iterable. In this article, we are focusing on arrays and strings. This means we will have two integers, usually named something like i and j, or left and right which each represent an index of the array or string.
Start the pointers at the edges of the input. Move them towards each other until they meet.
example codings: 2_pointer.py, square_of_sorted_array.py

